# Next.js carga `.env.local` (con punto). Origen del API sin duplicar /api/v1 (el código lo añade).
# También válido: URL ya terminada en /api/v1 (Docker) — ver `src/lib/api-base-url.ts`.
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_APP_NAME=GonsGarage
NEXTAUTH_SECRET=your-nextauth-secret
NEXTAUTH_URL=http://localhost:3000