// Package pkg holds cross-cutting, dependency-light utilities (template §1).
//
// Phase 1 establishes this tree per gonsgarage-rules; code currently lives under
// internal/platform (e.g. slogsetup) and will migrate here incrementally without
// importing application domain from shallow helpers.
package pkg
