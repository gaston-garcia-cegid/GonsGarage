# Archive report: admin-provision-user-roles

**Archived on**: 2026-04-20  
**Verify gate**: `verify-report.md` — no CRITICAL blockers (PASS WITH WARNINGS).

## Specs synced

| Domain | Action | Details |
|--------|--------|---------|
| `staff-user-provisioning` | **Created** | `openspec/specs/staff-user-provisioning/spec.md` — canonical spec promoted from change delta (with GonsGarage `POST /api/v1/admin/users` reference in Purpose). |
| `mvp-role-access` | **Updated** | Matrix row + footnote; expanded **Published role–surface matrix**; added **Staff user provisioning HTTP surface** + **CI coverage for provisioning authorization**; extended **CI authorization regression tests** with provisioning gate scenario. |

## Archive destination

`openspec/changes/admin-provision-user-roles/` → `openspec/changes/archive/2026-04-20-admin-provision-user-roles/`

## SDD cycle

Proposal → specs → tasks → apply → verify → **archive** (complete).
