# Archive report: workshop-mechanic-vehicle-lifecycle

**Data (ISO)**: 2026-04-22  
**Modo**: openspec (filesystem)

## Specs sincronizadas coa fonte de verdade

| Dominio | Acción |
|---------|--------|
| `workshop-repair-execution` | **Creado** `openspec/specs/workshop-repair-execution/spec.md` (desde o delta; nota de roles apunta a `mvp-role-access` principal) |
| `mvp-role-access` | **Modificado** `openspec/specs/mvp-role-access/spec.md`: fila de matriz *Workshop / service job*, requisito *Client cannot mutate* ampliado, escenarios novos, requisito *CI must fail if client mutates service job*, promoción no encabezado, matriz e *Published role–surface* actualizados, *CI authorization regression* co enlace a visita de taller |

## Contido arquivado (carpeta completa)

- `proposal.md` — si  
- `specs/**` (deltas) — si (conservados no histórico)  
- `design.md` — si  
- `tasks.md` — si  
- `verify-report.md` — si (PASS WITH WARNINGS)  
- `exploration.md` — si  

**SDD state**: o ciclo deste change (explore → … → verify → **archive**) quedou pechado. Próximos changes: novo cartafol bajo `openspec/changes/`.
