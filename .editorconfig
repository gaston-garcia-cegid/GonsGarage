# https://editorconfig.org
# Los .sql de migración deben ser UTF-8 sin BOM; Postgres falla con \ufeff al inicio del script.
root = true

[*.sql]
charset = utf-8
insert_final_newline = true
trim_trailing_whitespace = true
